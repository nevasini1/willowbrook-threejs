# Optional. Leave unset for local dev (Vite proxies /state, /agent, /ws to the backend).
# For production: your public API origin only — no trailing slash.
# Visitors never see this; you set it at build time (e.g. Netlify / Vercel env).
# VITE_API_BASE_URL=https://api.yourdomain.com
